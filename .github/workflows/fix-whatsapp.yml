name: Fix WhatsApp student message

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Patch admin page
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('app/admin/page.tsx')
          s = p.read_text()

          old_popup = "    const whatsappWindow = whatsappNumber ? window.open(`https://wa.me/${whatsappNumber}`, '_blank') : null;"
          new_popup = "    const whatsappWindow = whatsappNumber ? window.open('about:blank', '_blank') : null;"
          if old_popup in s:
              s = s.replace(old_popup, new_popup, 1)
              print('Patched WhatsApp popup')

          if 'const subjectNames = subjectIds' not in s:
              marker = "    e.currentTarget.reset(); setStudentLevel(''); await refresh();"
              if marker not in s:
                  raise SystemExit('Expected addStudent ending not found')
              new = '''    if (whatsappWindow && whatsappNumber) {
      const subjectNames = subjectIds
        .map((id) => subjects.find((subject: any) => String(subject.id) === String(id))?.name)
        .filter(Boolean)
        .join('، ') || 'Aucune';
      const clp = student.qr_code || 'Non disponible';
      const qrLink = clp ? `${window.location.origin}/eleve/${encodeURIComponent(clp)}` : '';
      const message = `السلام عليكم 👋\\n\\nمرحباً بكم في Centre Les Profs 📚\\n\\nتم تسجيل التلميذ بنجاح ✅\\n\\n👤 اسم التلميذ: ${full_name}\\n📱 رقم الهاتف: ${phone || 'غير متوفر'}\\n🎓 CLP: ${clp}\\n📚 المواد: ${subjectNames}\\n💰 المبلغ المؤدى: ${amount || 0} DH\\n📲 QR Code: ${qrLink || 'غير متوفر'}\\n\\nنتمنى له مسيرة دراسية موفقة ونجاحاً دائماً 🎓✨\\n\\nشكراً لثقتكم في Centre Les Profs ❤️`;
      whatsappWindow.location.href = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
    }
    e.currentTarget.reset(); setStudentLevel(''); await refresh();'''
              s = s.replace(marker, new, 1)
              print('Patched ready message')

          p.write_text(s)
          PY

      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/admin/page.tsx
          git diff --cached --quiet || git commit -m "fix: WhatsApp popup and ready student message"
          git push